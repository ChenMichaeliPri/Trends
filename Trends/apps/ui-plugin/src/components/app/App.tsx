import {Box} from "@mui/material";

import {MainPageController} from "../mainPage/MainPageController";

export const App = () => {
  return (
    <Box sx={{display:'flex' }}>
      <MainPageController/>
    </Box>
  );
}

